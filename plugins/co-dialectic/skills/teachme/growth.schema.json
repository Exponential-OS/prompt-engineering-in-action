{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Exponential-OS/co-dialectic/raw/main/plugins/co-dialectic/skills/teachme/growth.schema.json",
  "title": "Co-Dialectic Growth Telemetry \u2014 xOS-Hydratable Per-Turn and Tool-Lesson Record",
  "description": "Schema for a single line in ~/.codialectic/growth.jsonl. Each line is self-describing (per SHARED-STATE HYDRATION INVARIANT) so a future xOS consumer can ingest the file without co-dialectic source code. Prompt content is NEVER stored \u2014 only its full sha256 hash.",
  "version": "1.0.0",
  "type": "object",
  "required": [
    "schema_version",
    "event_type",
    "ts",
    "session_id"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1.0.0",
      "description": "Pinned to this schema's version. Bump on breaking schema changes; old consumers can still parse older lines by version."
    },
    "schema_url": {
      "type": "string",
      "format": "uri",
      "description": "Optional pointer to a hosted copy of the schema for runtime validation. Consumers can fetch this URL to validate without bundling the schema."
    },
    "event_type": {
      "type": "string",
      "enum": [
        "sharpening_turn",
        "micro_lesson_surfaced",
        "micro_lesson_dismissed",
        "technique_practiced",
        "session_summary",
        "tool_lesson"
      ],
      "description": "Discriminator for downstream aggregation. Most lines will be sharpening_turn."
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp. Always include the Z suffix."
    },
    "session_id": {
      "type": "string",
      "pattern": "^[a-f0-9]{8}$",
      "description": "8-char hex session identifier. Aggregates within a single Co-Dialectic session; rotates across sessions."
    },
    "persona": {
      "type": "string",
      "description": "Active persona slug (e.g., architecture, product, design, debugging) at the time of the turn."
    },
    "stakes_tier": {
      "type": "string",
      "enum": [
        "T0",
        "T1",
        "T2",
        "T3",
        "T4"
      ],
      "description": "EMERGENT SYSTEM IMMUNITY stakes tier detected for this prompt. T3+ triggers eager dialectic synthesis."
    },
    "prompt_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "sha256(prompt_text) \u2014 full 64-char hex. SHA-256 is cryptographically one-way; the prompt is NEVER recoverable from the hash. Privacy invariant."
    },
    "prompt_length_chars": {
      "type": "integer",
      "minimum": 0,
      "description": "Length of the original prompt in characters (no content)."
    },
    "original_score": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "Prompt-quality score before sharpening, 0-100."
    },
    "tiers": {
      "type": "object",
      "description": "Per-tier scores, techniques, and cost. The user's choice is recorded in user_picked.",
      "required": [
        "improved"
      ],
      "properties": {
        "improved": {
          "$ref": "#/definitions/tierRecord"
        },
        "socratic": {
          "$ref": "#/definitions/tierRecord"
        },
        "dialectic": {
          "$ref": "#/definitions/tierRecord"
        }
      }
    },
    "user_picked": {
      "type": "string",
      "enum": [
        "improved",
        "socratic",
        "dialectic",
        "original",
        "edited"
      ],
      "description": "Which tier the user chose. 'original' means user kept their prompt unchanged. 'edited' means user provided a custom edit."
    },
    "tier_used_cost_usd": {
      "type": "number",
      "minimum": 0,
      "description": "Actual cost of the path the user chose. For dialectic, this includes the synthesis round."
    },
    "calibration_score": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "Cal: score on the response that followed the sharpening."
    },
    "dismissed_micro_lessons": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Technique slugs the user has muted in this session."
    },
    "cited_log_evidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/logEvidenceRecord"
      },
      "description": "Local evidence used for a tool_lesson. References ledger/git/config/hook signals by source and hash; raw prompt content is never stored."
    },
    "inefficiency_class": {
      "type": "string",
      "enum": [
        "manual-where-tool-exists",
        "base-use-of-power-tool",
        "repeated-friction",
        "installed-never-invoked",
        "approval-grind"
      ],
      "description": "The human-side tool-usage inefficiency class detected by the co-education audit."
    },
    "tool": {
      "type": "string",
      "minLength": 1,
      "description": "Installed tool, skill, native command, MCP, preset, or workflow that would have served the user's intent at higher leverage."
    },
    "yield_estimate": {
      "$ref": "#/definitions/yieldEstimate",
      "description": "Estimated lesson yield, calculated as time burned multiplied by recurrence."
    },
    "taught_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp when the tool lesson was taught."
    },
    "adopted": {
      "type": [
        "boolean",
        "null"
      ],
      "description": "Whether the next audit found adoption. null means not checked yet."
    },
    "teach_count": {
      "type": "integer",
      "minimum": 1,
      "description": "How many times this lesson has been taught. Two teaches without adoption escalates to hookify-candidate."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "event_type": {
            "const": "sharpening_turn"
          }
        },
        "required": [
          "event_type"
        ]
      },
      "then": {
        "required": [
          "prompt_hash",
          "original_score",
          "tiers"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "event_type": {
            "const": "tool_lesson"
          }
        },
        "required": [
          "event_type"
        ]
      },
      "then": {
        "required": [
          "cited_log_evidence",
          "inefficiency_class",
          "tool",
          "yield_estimate",
          "taught_at",
          "adopted",
          "teach_count"
        ]
      }
    }
  ],
  "definitions": {
    "tierRecord": {
      "type": "object",
      "required": [
        "score",
        "technique"
      ],
      "properties": {
        "score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Estimated quality score for this tier's rewrite."
        },
        "technique": {
          "type": "string",
          "enum": [
            "specificity-injection",
            "role-priming",
            "audience-priming",
            "constraint-injection",
            "chain-of-thought",
            "few-shot-by-example",
            "flipped-interaction",
            "socratic-questioning",
            "thesis-antithesis-synthesis",
            "meta-shortcut",
            "alternative-approaches"
          ],
          "description": "The technique label applied at this tier. Used by teachme to look up the deep-dive file."
        },
        "cost_usd": {
          "type": "number",
          "minimum": 0,
          "description": "Estimated USD cost of generating this tier's rewrite. Dialectic synthesis is included only if the tier was actually generated (eager mode or user-picked)."
        }
      }
    },
    "logEvidenceRecord": {
      "type": "object",
      "required": [
        "source_type",
        "source_ref",
        "signal",
        "evidence_hash"
      ],
      "properties": {
        "source_type": {
          "type": "string",
          "enum": [
            "ledger",
            "git",
            "installed_tool_inventory",
            "skill_version_banner",
            "hook_signal",
            "config",
            "conversation_context"
          ],
          "description": "Local evidence source category."
        },
        "source_ref": {
          "type": "string",
          "description": "Local file path + line, commit SHA, hook trace id, or config path. No raw prompt content."
        },
        "signal": {
          "type": "string",
          "description": "Short redacted signal explaining what the evidence showed."
        },
        "evidence_hash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "sha256 of the cited local snippet or event payload. Stores proof identity without storing prompt content."
        }
      }
    },
    "yieldEstimate": {
      "type": "object",
      "required": [
        "time_burned_minutes",
        "recurrence_count",
        "score"
      ],
      "properties": {
        "time_burned_minutes": {
          "type": "number",
          "minimum": 0,
          "description": "Estimated minutes burned by the inefficiency in this audit window."
        },
        "recurrence_count": {
          "type": "integer",
          "minimum": 1,
          "description": "How many times the same friction pattern appeared."
        },
        "score": {
          "type": "number",
          "minimum": 0,
          "description": "time_burned_minutes multiplied by recurrence_count."
        }
      }
    }
  }
}
